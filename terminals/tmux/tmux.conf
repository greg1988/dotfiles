set -g default-terminal "xterm-256color"

# base16-powerline
source-file "${HOME}/.tmux/base16-tmux/base16.tmuxtheme"

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Override window config so we include the window index
set -g window-status-format " #I #W "
